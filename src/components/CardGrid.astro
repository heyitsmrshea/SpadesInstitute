--- 
import Card from './Card.astro';
const { items = [] } = Astro.props;
--- 
<div class="grid gap-8 md:grid-cols-2 lg:grid-cols-3">
  {items.map((item, i) => (
    <div class="motion-safe:animate-fadeUp" style={`animation-delay: ${i * 150}ms`}>
      <Card {...item} />
    </div>
  ))}
</div>
